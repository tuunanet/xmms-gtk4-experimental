stereo_module = shared_module('stereo', files('stereo.c'), dependencies: gtk2_dep, include_directories: [xmms_include_dir, xmms_i18n_include_dir], install: true, install_dir: xmms_plugin_dir / 'Effect')
