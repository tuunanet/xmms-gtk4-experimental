schema_version: 1
story_id: e03s03
title: Prove the Play-button boundary in a separate GTK3 harness
status: failing
risk: P0
bcps: 8
tasks:
  - id: t1
    scenarios: [SC-e03s03-P0-01]
    description: Add authoritative optional GTK3 detection and a separately linked GTK3 proof target without changing the production GTK2 target.
    verify: "./configure --disable-esd && make -C tests test-gtk3-play-button-proof && ldd tests/test-gtk3-play-button-proof | grep -F 'libgtk-3.so' && ! ldd tests/test-gtk3-play-button-proof | grep -F 'libgtk-x11-2.0.so'"
    risk: P0
    security: low
    status: failing
    allure:
      severity: critical
      categories: [gtk-migration, gtk3, build-boundary]
  - id: t2
    scenarios: [SC-e03s03-P0-02, SC-e03s03-P0-03]
    description: Render normal and pressed Play-button states and activate the callback in the GTK3 proof process through the shared toolkit-neutral contract.
    verify: "xvfb-run --auto-servernum tests/test-gtk3-play-button-proof"
    risk: P0
    security: none
    status: failing
    allure:
      severity: critical
      categories: [gtk-migration, gtk3, rendering, interaction]
  - id: t3
    scenarios: [SC-e03s03-P0-04]
    description: Add GTK3 development prerequisites and proof execution to applicable local, CI, packaging, clean, and distribution paths while preserving the GTK2 application package.
    verify: "tests/test-package-recipes.sh \"$PWD\" && make lint && xvfb-run --auto-servernum make check && xvfb-run --auto-servernum make distcheck"
    risk: P0
    security: low
    status: failing
    allure:
      severity: critical
      categories: [gtk-migration, ci, packaging, distribution]
  - id: t4
    scenarios: [SC-e03s03-P0-01, SC-e03s03-P0-04]
    description: Update architecture and plugin documentation with the proven process boundary, staged migration status, and GTK-major plugin UI policy.
    verify: "rg -q 'GTK2.*GTK3.*GTK4' docs/architecture/ui-interaction.md && rg -q 'mixed.*GTK|GTK.*major' docs/architecture/plugin-system.md"
    risk: P1
    security: none
    status: failing
    allure:
      severity: high
      categories: [gtk-migration, documentation, compatibility]
